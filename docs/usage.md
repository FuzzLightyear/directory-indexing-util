# CLI Usage

The demos below run the real CLI against a synthetic directory tree and are re-rendered on every deploy, so they always show current behavior.

## Scanning

Enumerate files into a structured index, optionally filtered by extension:

![dirindex scan demo](assets/scan.gif)

## One-shot indexing

Scan and hash in a single command, with a progress bar and a provenance manifest:

![dirindex index demo](assets/index.gif)

## Profiles

Save the *how* of a run once, reuse it anywhere by name:

![dirindex profile demo](assets/profiles.gif)

## Reference

--8<-- "README.md:cli"
